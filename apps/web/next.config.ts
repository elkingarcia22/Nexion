import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  // Phase 2: add Supabase image domain when auth is active
  // images: { remotePatterns: [{ protocol: 'https', hostname: '*.supabase.co' }] },
}

export default nextConfig
